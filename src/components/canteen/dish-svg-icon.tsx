import type { LucideIcon } from "lucide-react"; import {   CakeSliceIcon,   CookingPotIcon,   CupSodaIcon,   FlameIcon,   SoupIcon,   UtensilsCrossedIcon,   WheatIcon, } from "lucide-react"; import { cn } from "@/lib/utils"; import { resolveDishSvgKey, type DishSvgKey } from "@/lib/canteen-svg-keys";  /** Stroke icons tuned for ~24px: clear silhouette at menu-row size. */ const DISH_ICONS: Record<DishSvgKey, LucideIcon> = {   default: UtensilsCrossedIcon,   rice: WheatIcon,   bowl: SoupIcon,   spicy: FlameIcon,   noodle: CookingPotIcon,   drink: CupSodaIcon,   dessert: CakeSliceIcon, };  export function DishSvgIcon({   svgKey = "default",   className, }: {   svgKey?: string;   className?: string; }) {   const key = resolveDishSvgKey(svgKey);   const Icon = DISH_ICONS[key];   return (     <div       className={cn(         "flex size-12 shrink-0 items-center justify-center rounded-md bg-[var(--canteen-tray)]/70 ring-1 ring-[var(--canteen-line)]",         className,       )}       aria-hidden       data-svg-key={key}     >       <Icon         className="size-6 text-[var(--canteen-purple)]"         strokeWidth={2}         absoluteStrokeWidth       />     </div>   ); }